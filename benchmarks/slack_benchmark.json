{
  "config": "config.json",
  "benchmarks": [
    {
      "landmarks": [],
      "input_args": {
        "channel_name": "objs_conversation.name"
      },
      "output": "objs_user.profile.email",
      "solution": [
        "(channel_name) => {let x11 = /users.conversations();    ",
        " let x12 = x11.channels.filter(x => x.latest.name == channel_name);    ",
        " let x18 = x12.map((x2) => {let x14 = /users.info(user=x2.creator);    ",
        " return x14.user.profile.email;});    ",
        " return x18;}"
      ],
      "max_length": 5
    },
    {
      "landmarks": [],
      "input_args": {
        "email": "objs_user.profile.email"
      },
      "output": "objs_message",
      "max_length": 5
    }
  ]
}
